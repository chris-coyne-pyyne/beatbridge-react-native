import {Text, View, Button} from 'react-native';

import {globalStyles} from '../../styles/Styles';

export function SignupScreen({navigation}) {
  return (
    <View style={globalStyles.container}>
      <Text style={globalStyles.paragraph}>Signup</Text>
    </View>
  );
}
